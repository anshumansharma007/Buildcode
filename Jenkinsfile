pipeline {
     agent any
     stages {
         stage('Build step') {
              steps {
                 echo "Build stage is running"
              }
         }
     
     stages {
         stage('Test step') {
              steps {
                 echo "Test stage is running"
              }
         }     
     
     stages {
         stage('Deploy step') {
              steps {
                 echo "Deployment stage is running"
              }
         }
     }
}
