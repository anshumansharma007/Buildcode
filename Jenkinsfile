pipeline {

     agent any
     
     stages {

         stage('Build step') {
              
              steps {
                 echo "Build stage is running"
              }
         }
     
         stage('Test step') {
              
              steps {
                 echo "Test stage is running"
              }
         }     
     
         stage('Deploy step') {
              
              steps {
                 echo "Deployment stage is running"
              }
         }    
     }
}
